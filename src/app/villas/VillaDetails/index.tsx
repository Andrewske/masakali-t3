import React from 'react';

const VillaDetails = () => {
  const headings = ['description', 'ammenities', 'reviews'];
  const [activeHeading, setActiveHeading] = useState(headings[0]);

  return <div>VillaDetails</div>;
};

export default VillaDetails;
